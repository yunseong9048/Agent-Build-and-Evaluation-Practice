---
name: stock-news-analysis
description: 특정 종목·기업의 최신 뉴스를 웹에서 수집·요약하고 주가 영향(방향·영향도)을 근거와 함께 분석해 투자 참고 브리핑을 만든다. "종목 뉴스 분석", "주가 영향", "OO 최근 뉴스 정리", "개장 전 브리핑" 같은 요청에 사용. 투자 매수/매도 추천은 하지 않는다.
license: MIT
---

# 주식 뉴스 분석 스킬

특정 종목/기업의 최신 뉴스를 근거 기반으로 수집·요약하고, **주가 영향 가능성**을 방향(긍정/부정/중립)과 영향도 점수로 정리해 투자 **참고용** 브리핑을 만든다. 이 스킬은 투자 자문이 아니며, 매수·매도·보유를 추천하지 않는다.

## 언제 쓰나

- "삼성전자 최근 뉴스 분석해줘", "테슬라 최신 뉴스 알려줘" 같은 종목/기업 뉴스 요청
- "SK하이닉스 개장 전 뉴스 브리핑", "AI 반도체 관련 주요 뉴스 요약" 같은 산업/종목 동향 정리
- 뉴스가 주가에 어떤 영향을 줄지 근거와 함께 판단이 필요할 때

## 절차

1. **대상·범위 확정**
   - 종목명/기업명(티커 있으면 함께), 기간(기본: 최근 7일. "개장 전"이면 전일 장마감~당일 개장 전), 관심 축(실적/공급망/경쟁사/정책 등)을 확정한다.
   - 불명확하면 최소 한 가지만 되묻고, 나머지는 합리적 기본값으로 진행한다.

2. **뉴스 수집 (웹 검색 필수)**
   - 웹 검색 커넥터(Tavily 등)를 **최소 2~3회**, 키워드를 바꿔가며 호출한다.
     - 예) "<기업명> 실적", "<기업명> 공급망/수주", "<기업명> 규제/정책", "<티커> news".
   - 금융 토픽은 `topic="finance"`가 유효하다(단 `search_depth`는 `advanced` 사용).
   - 각 기사마다 제목 / 발행일(추정 시 표기) / 출처명 / URL / 핵심 사실을 기록한다.

3. **중복 제거·선별**
   - 같은 사건을 다룬 기사는 하나로 묶는다(가장 원문에 가까운 출처 우선).
   - 대상 종목과 관련성이 낮은 일반 뉴스는 제외한다.
   - 상위 5~10건을 영향도 순으로 선정한다.

4. **영향 분석 (항목별)**
   각 항목에 대해 아래를 채운다. 추정과 사실을 분리한다.
   - **관련성**: 대상 종목과 어떻게 연결되는가(직접/간접 구분).
   - **영향 방향**: 긍정 / 부정 / 중립. 단정 표현("반드시 오른다")은 쓰지 않는다.
   - **영향도**: 1~5점 + 한 줄 근거(왜 그 점수인지).

5. **브리핑 작성**
   `templates/briefing.md` 형식을 따른다. 핵심 요약을 맨 앞에 둔다. 길거나 재사용될 것 같으면 `write_file`로 `workspace/`에 저장하고 경로를 알린다.

6. **자기 점검** (`references/rules.md` 체크리스트)
   - 모든 항목에 출처 URL이 있는가 · 원문에 없는 수치를 지어내지 않았는가 · 투자 추천 표현이 없는가 · 확인 안 된 정보에 `[확인 필요]`를 붙였는가.

## 반드시 지키는 규칙 (요약)

- **출처 없는 항목은 브리핑에 넣지 않는다.** 모든 항목에 URL 필수.
- **원문에 없는 숫자·사실·발언을 지어내지 않는다.** 불확실하면 `[확인 필요]`.
- **매수/매도/보유를 추천하지 않는다.** 주가 방향을 단정하지 않는다.
- **영향 방향·영향도에는 반드시 근거를 붙인다.**
- **루머는 넣지 않거나 `[확인 필요]`로만.** 미공개 내부정보를 추정·생성하지 않는다.
- **한국어로 간결하게.** 고유명사(기업명/모델명)는 원문 표기 유지.

자세한 규칙과 템플릿은 `references/rules.md`, `templates/briefing.md` 참조.
